# Do not delete. This file is used to configure 
# python black formatting
[tool.black]
line-length = 79

[project]
name = "pybackup"
version = "0.1.0"
description = "Python backup script"

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]
